# Plan: Ajustes al scope de diagramas

## Cambios solicitados

1. **C4: solo DSL, sin generación de PNGs** — Los 5 archivos .puml de C4 se mantienen como fuente de contexto para IA, pero no se generan PNGs desde ellos.
2. **Generación manual de diagrams** — El target `diagrams` es independiente, no dependencia de `pdf` o `single`.

## Archivos a modificar

### 1. `Makefile`
- **Eliminar** C4 del target `diagrams` (solo class-diagrams)
- **Eliminar** `diagrams` como dependencia de `pdf` y `single`
- Resultado: `make diagrams` es un target manual que solo genera class diagram PNGs

### 2. C4 `.puml` — Revertir includes a remotos
Revertir los includes `<C4/C4_*>` de vuelta a los URLs remotos originales (que ya funcionaban). Mantener solo el typo fix `marketplaceIntifervention` → `marketplaceIntervention` en `VioraApiComponents.puml`.

Archivos:
- `VioraSystemContext.puml` — restaurar `!include https://raw.githubusercontent.com/plantuml-stdlib/C4/master/C4_Context.puml`
- `VioraContainer.puml` — restaurar `!include https://raw.githubusercontent.com/plantuml-stdlib/C4/master/C4_Container.puml`
- `VioraApiComponents.puml` — restaurar include remoto + mantener typo fix
- `VioraWebAppComponents.puml` — restaurar include remoto
- `VioraLandingComponents.puml` — restaurar include remoto

### 3. Class diagrams `.puml` — Ya corregidos (sin cambios adicionales)
Los 17 archivos de class-diagrams ya fueron corregidos en la ejecución anterior. No requieren más cambios.

### 4. `47-software-object-oriented-design.md` — Ya corregido
Las rutas de `\includegraphics` ya fueron corregidas. No requiere más cambios.

## Verificación
1. `make diagrams` genera solo los 17 class diagram PNGs
2. `make pdf` NO ejecuta diagram generation automáticamente
3. Los .puml de C4 mantienen su include remoto y el typo fix
